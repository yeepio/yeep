import React from 'react';
import useDocumentTitle from '@rehooks/document-title';

const PermissionList = () => {
  useDocumentTitle('Permission List');
  return (
    <React.Fragment>
      <h3>Permission List (WIP)</h3>
    </React.Fragment>
  );
};

export default PermissionList;
