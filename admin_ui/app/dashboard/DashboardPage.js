import React from 'react';
import { Router, Link } from '@reach/router';

const DashboardPage = () => {
  return (
    <React.Fragment>
      <h1>Dashboard</h1>
    </React.Fragment>
  );
};

export default DashboardPage;